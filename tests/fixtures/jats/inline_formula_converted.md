# Abstract

The abstract preserves $E = mc^2$ inline.

## Body

- Outer finding $x_i = y_i$.
  1. Ordered evidence {math}`a_i + b_i = c_i`.
  2. A prose-only control.
     - Nested evidence \(p = q+r\).

> A quoted content layer keeps $z = 3$.

```text
<inline-formula><tex-math>x = hidden</tex-math></inline-formula>
```

<!--
Fixture provenance/license: synthetic, hand-authored converted Markdown for
JATS-shaped content; it is not copied from or generated from an external
JATS/XML source. No third-party text or assets are embedded; the repository's
MIT license applies.
-->
